services:
  redis:
    image: redis/redis-stack:latest
    container_name: valkyrie-redis
    ports:
      - "6379:6379" 
      - "8001:8001" # RedisInsight web GUI

    volumes:

      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
